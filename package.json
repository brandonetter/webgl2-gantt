{
  "name": "gantt-webgl2-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @gantt/demo",
    "build": "npm run build -w @gantt/demo",
    "preview": "npm run preview -w @gantt/demo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
